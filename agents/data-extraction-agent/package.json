{
  "name": "@openclawio/data-extraction-agent",
  "version": "1.0.0",
  "description": "Document and data extraction agent for OpenClaw swarm",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --loader ts-node/esm src/index.ts",
    "dev": "nodemon --exec node --loader ts-node/esm src/index.ts",
    "build": "tsc --noEmit",
    "test": "echo 'No tests yet'"
  },
  "keywords": ["agent", "openclaw", "extraction", "parsing"],
  "author": "OpenClaw Team",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
